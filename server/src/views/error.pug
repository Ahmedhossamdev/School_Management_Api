doctype html
html(lang="en")
    head
        meta(charset="utf-8")
        title Error
    body
        h1 Error
        if message
            p= message
        if error.stack
            pre= error.stack
